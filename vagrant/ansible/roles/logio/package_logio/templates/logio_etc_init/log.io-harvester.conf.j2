description "Log.io Harvester"

start on runlevel [2345]
stop on runlevel [!2345]

respawn

exec sudo -u {{ fact_logio_user_environment_user }} -H $(which log.io-harvester) 1>{{ role_package_logio__var__logs_folder }}/harvester.log 2>{{ role_package_logio__var__logs_folder }}/harvester.err
